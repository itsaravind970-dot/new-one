workflows:
  build-my-app:
    name: Build Application
    environment:
      node: latest
    scripts:
      - name: Install dependencies
        script: npm install
      - name: Build project
        script: npm run build
    artifacts:
      - dist/**
      
